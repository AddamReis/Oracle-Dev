--FORMAS DE CRIAR UMA PK
CREATE TABLE TALUNO
(
ID_ALUNO INTERGER NOT NULL
PRIMARY KEY (ID_ALUNO)

OR

ID_ALUNO INTEGER PRIMARY KEY NOT NULL
)

VALOR NUMBER(8,2) -- IGUAL A 6 CASAS ANTES DA VÍRGULA E 2 DEPOIS

ALTER TABLE *TABELA* ADD NM_COLUNA VARCHAR(50); --CRIAR COLUNA
ALTER TABLE *TABELA* DROP COLUMN NM_COLUMN; --EXCLUI COLUNA
DROP TABLE *TABELA*

ALTER TABLE *TABELA* RENAME COLUMN *NM_ATUAL_COLUNA* TO *NOVO_NOME_COLUNA*; --RENOMEAR COLUNA 

--SEQUENCE--
CREATE SEQUENCE SEQ_NOME START WITH 5; --CRIA SEQUENCE QUE O ID COMEÇA A SER IMPLEMENTADO A PARTIR DO 5
*SEQ_NOME.NEXTVAL* --DECLARAR AUTOINCREMENTO NOS IDS DO INSERT
SELECT SEQ_ALUNO.CURRVAL FROM DUAL; --RETORNA O ULTIMO ID IMPLEMENTADO DA SEQUENCE
------------

COMMIT; --CONFIRMA TODAS AS AÇÕES REALIZADAS
ROLLBACK;-- EXCLUÍ TODAS AS AÇÕES REALIZADAS

ALT + 3 --RETORNA TODAS AS TABELAS CRIADAS
