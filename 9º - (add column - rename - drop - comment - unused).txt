SELECT * FROM USER_TABLES; --EXIBE TODAS AS TABELAS CRIADAS PELO USUÁRIO ATUAL

SELECT * FROM ALL_TABLES; --EXIBE TODAS AS TABELAS DO SISTEMA

ALTER TABLE *TABELA* ADD *NOME_COLUNA* NUMBER(10,8) DEFAULT 0 --INCLUI UMA COLUNA

ALTER TABLE *TABELA* RENAME COLUMN *NOME_COLUNA* TO *NOVO_NOME* --RENOMEIA UMA COLUNA

ALTER TABLE *TABELA* DROP COLUMN *NOME_COLUNA* --DELETA COLUNA

DROP TABLE *TABELA* --EXCLUI A TABELA 

RENAME *TABELA* TO *NOVO_NOME*
                                                                      
COMMENT ON TABLE *TABELA* IS 'COMENTÁRIO EXPLICANDO O QUE A TABELA ARMAZENA' --EX: CONTRATOS, PESSOAS, CURSOS

COMMENT ON COLUMN *NOME_COLUNA* IS 'COMENTÁRIO EXPLICANDO O QUE A COLUNA ARMAZENA' --EX: SEXO, IDADE, NOME

SELECT * FROM USER_COL_COMMENTS WHERE TABLE_NAME = 'NOME_TABELA' --RETORNA OS COMENTÁRIOS DAS COLUNAS DA TABELA SELECIONADA

SELECT * FROM USER_TAB_COMMENTS WHERE TABLE_NAME = 'NOME_TABELA' --RETORNA O COMENTÁRIO DA TABELA

ALTER TABLE *TABELA* SET UNUSED (NOME_COLUNA) --DESABILITA COLUNA, NÃO É POSSÍVEL REVERTER TAL AÇÃO

ALTER TABLE *TABELA* DROP UNUSED COLUMNS --DELETA TODAS AS COLUNAS QUE ESTIVEM DESABILITADAS 
