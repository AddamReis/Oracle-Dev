--MERGE É UTILIZADO PARA ATUALIZAR REGISTROS JÁ EXISTENTES DE UMA OU MAIS TABELAS, E INSERIR TAMBÉM
MERGE INTO *TABELA1* T1
USING (SELECT COD AS CODIGO FROM *TABELA2*)
ON (T1.COD = CODIGO)
WHEN MATCHED THEN --SE ENCONTRAR REGISTRO
UPDATE SET *COLUNA* = *CONDIÇÃO*
WHEN NOT MATCHED THEN --NÃO ENCONTROU REGISTRO
INSERT... VALUES...;
